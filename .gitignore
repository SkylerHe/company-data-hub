# Database (don't commit the binary; CI uses cache / artifacts — see HANDOFF step 5)
finance.db
*.db
*.db-wal
*.db-shm

# Backups (large binary DB snapshots + config backups)
*.bak
*.bak-*

# Environment variables (contains SEC_IDENTITY email)
.env

# Logs
*.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude Code local settings
.claude/settings.local.json
